name: Post News to Facebook

on:
  schedule:
    - cron: '0 */3 * * *' # This runs every 3 hours
  workflow_dispatch:

jobs:
  post_news:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests facebook-sdk schedule

    - name: Run script
      env:
        NEWS_API_KEY: ${{pub_49586889780b6419f1834124d8951f107a8e3}}
        FACEBOOK_ACCESS_TOKEN: ${{pub_49586889780b6419f1834124d8951f107a8e3}}
      run: |
        python logo.py
